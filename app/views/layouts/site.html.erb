<!DOCTYPE html>
<html class="h-full bg-gray-800">
  <head>
    <%= render partial: 'layouts/shared/head' %>
  </head>
  <body class="h-full">
    <div class="min-h-full">
      <main class="">
        <% resource_class.omniauth_providers.each do |provider| %>
          <%= provider_links(omniauth_authorize_path(resource_name, provider), provider) %>
        <% end %>
      </main>
    </div>
  </body>
</html>
