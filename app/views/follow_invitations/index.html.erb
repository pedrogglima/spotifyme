<div class="bg-white rounded-lg shadow px-5 py-6 sm:px-6">
  <div class="">
    <%= turbo_frame_tag 'follow_invitations', class: "flex flex-col space-y-4" do %>
        <!-- TODO: fix msg 'You have...' for when a async invitation is added (msg still there) -->
        <!--   uses before_action stimulus/hotwire to trigger func. for removing msg if present -->
        <%= render(@invitations, cached: true) || 'You have no invitation!' %>

        <%== pagy_nav(@pagy) %>
      </div>
    <% end %>
  </div>
</div>

